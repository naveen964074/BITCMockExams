{
  "version": "0.2.0",
  "configurations": [

    
 
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "url": "http://localhost:5174",
      "webRoot": "${workspaceFolder}"
    },
       {
      "type": "pwa-msedge",
      "request": "launch",
      "name": "localhost (Edge)",
      "url": "http://localhost:5174/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
